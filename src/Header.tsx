import React from "react";

function Header () {
    return (
        <header>
            <h1>E-coleta</h1>
        </header>
    ); /*Cuidado: parêntesis, não chaves, porque não é um objeto, é só uma quebra de linha.*/
}

export default Header;